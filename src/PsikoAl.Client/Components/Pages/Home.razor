@page "/"
@inject AdminSessionState Session
@inject NavigationManager Navigation

<PageTitle>PsikoAl Admin</PageTitle>

@code {
    protected override void OnInitialized()
        => Navigation.NavigateTo(Session.IsAuthenticated ? "/users" : "/login");
}
